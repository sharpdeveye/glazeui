@page "/components"

<PageTitle>Components — GlazeUI</PageTitle>
<HeadContent>
    <meta name="description" content="Browse all 34 GlazeUI components — atoms, molecules, and organisms. Live interactive examples with all variants, sizes, and states." />
</HeadContent>

@* === Page Header === *@
<section class="border-b border-border/50 py-12 sm:py-16">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
        <p class="text-sm font-medium text-primary">Components</p>
        <h1 class="mt-2 text-3xl font-bold tracking-tight text-foreground sm:text-4xl">
            Component Library
        </h1>
        <p class="mt-3 max-w-2xl text-lg text-muted-foreground">
            34 components across 3 tiers — atoms, molecules, and organisms. Every component ships with semantic tokens, full state coverage, keyboard accessibility, and TailwindMerge class composition.
        </p>
        <div class="mt-4 flex items-center gap-3">
            <GzButton Variant="@(_isRtl ? ButtonVariant.Default : ButtonVariant.Outline)" Size="ButtonSize.Sm" OnClick="@(() => _isRtl = !_isRtl)">
                @(_isRtl ? "RTL Mode" : "LTR Mode")
            </GzButton>
            <span class="text-xs text-muted-foreground">Toggle to preview all components in RTL layout</span>
        </div>
    </div>
</section>

@* === Components with Sidebar === *@
<section class="py-12 sm:py-16">
    <div class="mx-auto flex max-w-6xl gap-10 px-4 sm:px-6 lg:px-8">

        @* ─── Sidebar Nav ─────────────────────── *@
        <aside class="hidden w-48 shrink-0 lg:block">
            <nav class="sticky top-24 max-h-[calc(100vh-7rem)] space-y-1 overflow-y-auto pb-8">
                <p class="mb-2 text-xs font-medium uppercase tracking-widest text-muted-foreground">Atoms</p>
                <GzSidebarLink Href="/components#button" Label="Button" />
                <GzSidebarLink Href="/components#badge" Label="Badge" />
                <GzSidebarLink Href="/components#separator" Label="Separator" />
                <GzSidebarLink Href="/components#input" Label="Input" />
                <GzSidebarLink Href="/components#textarea" Label="Textarea" />
                <GzSidebarLink Href="/components#checkbox" Label="Checkbox" />
                <GzSidebarLink Href="/components#radio" Label="Radio" />
                <GzSidebarLink Href="/components#switch" Label="Switch" />
                <GzSidebarLink Href="/components#avatar" Label="Avatar" />
                <GzSidebarLink Href="/components#typography" Label="Typography" />
                <GzSidebarLink Href="/components#skeleton" Label="Skeleton" />
                <GzSidebarLink Href="/components#progress" Label="Progress" />
                <GzSidebarLink Href="/components#slider" Label="Slider" />

                <p class="mb-2 mt-4 text-xs font-medium uppercase tracking-widest text-muted-foreground">Molecules</p>
                <GzSidebarLink Href="/components#card" Label="Card" />
                <GzSidebarLink Href="/components#alert" Label="Alert" />
                <GzSidebarLink Href="/components#formfield" Label="FormField" />
                <GzSidebarLink Href="/components#tabs" Label="Tabs" />
                <GzSidebarLink Href="/components#tooltip" Label="Tooltip" />
                <GzSidebarLink Href="/components#select" Label="Select" />
                <GzSidebarLink Href="/components#accordion" Label="Accordion" />
                <GzSidebarLink Href="/components#breadcrumb" Label="Breadcrumb" />
                <GzSidebarLink Href="/components#fileupload" Label="FileUpload" />
                <GzSidebarLink Href="/components#stepper" Label="Stepper" />

                <p class="mb-2 mt-4 text-xs font-medium uppercase tracking-widest text-muted-foreground">Organisms</p>
                <GzSidebarLink Href="/components#dialog" Label="Dialog" />
                <GzSidebarLink Href="/components#dropdown" Label="DropdownMenu" />
                <GzSidebarLink Href="/components#popover" Label="Popover" />
                <GzSidebarLink Href="/components#toast" Label="Toast" />
                <GzSidebarLink Href="/components#combobox" Label="Combobox" />
                <GzSidebarLink Href="/components#datatable" Label="DataTable" />
                <GzSidebarLink Href="/components#datepicker" Label="DatePicker" />
            </nav>
        </aside>

        @* ─── Component Sections ──────────────── *@
        <div class="min-w-0 flex-1 space-y-16" dir="@(_isRtl ? "rtl" : "ltr")">

        @* ─── Button ─────────────────────────────────── *@
        <div id="button" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Button</h2>
                <p class="mt-1 text-sm text-muted-foreground">6 variants × 4 sizes. Loading state, icon slots, tactile press, and premium inset-shadow depth.</p>
            </div>

            <div class="space-y-8 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                @* Variants *@
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Variants</p>
                    <div class="flex flex-wrap items-center gap-3">
                        <GzButton>@L("Default", "افتراضي")</GzButton>
                        <GzButton Variant="ButtonVariant.Secondary">@L("Secondary", "ثانوي")</GzButton>
                        <GzButton Variant="ButtonVariant.Destructive">@L("Destructive", "تدميري")</GzButton>
                        <GzButton Variant="ButtonVariant.Outline">@L("Outline", "محيط")</GzButton>
                        <GzButton Variant="ButtonVariant.Ghost">@L("Ghost", "شبحي")</GzButton>
                        <GzButton Variant="ButtonVariant.Link">@L("Link", "رابط")</GzButton>
                    </div>
                </div>

                <GzSeparator />

                @* Sizes *@
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Sizes</p>
                    <div class="flex flex-wrap items-center gap-3">
                        <GzButton Size="ButtonSize.Sm">@L("Small", "صغير")</GzButton>
                        <GzButton Size="ButtonSize.Md">@L("Medium", "متوسط")</GzButton>
                        <GzButton Size="ButtonSize.Lg">@L("Large", "كبير")</GzButton>
                        <GzButton Size="ButtonSize.Icon" aria-label="Settings">
                            <svg class="size-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M9.594 3.94c.09-.542.56-.94 1.11-.94h2.593c.55 0 1.02.398 1.11.94l.213 1.281c.063.374.313.686.645.87.074.04.147.083.22.127.325.196.72.257 1.075.124l1.217-.456a1.125 1.125 0 011.37.49l1.296 2.247a1.125 1.125 0 01-.26 1.431l-1.003.827c-.293.241-.438.613-.43.992a7.723 7.723 0 010 .255c-.008.378.137.75.43.991l1.004.827c.424.35.534.955.26 1.43l-1.298 2.247a1.125 1.125 0 01-1.369.491l-1.217-.456c-.355-.133-.75-.072-1.076.124a6.47 6.47 0 01-.22.128c-.331.183-.581.495-.644.869l-.213 1.281c-.09.543-.56.94-1.11.94h-2.594c-.55 0-1.019-.398-1.11-.94l-.213-1.281c-.062-.374-.312-.686-.644-.87a6.52 6.52 0 01-.22-.127c-.325-.196-.72-.257-1.076-.124l-1.217.456a1.125 1.125 0 01-1.369-.49l-1.297-2.247a1.125 1.125 0 01.26-1.431l1.004-.827c.292-.24.437-.613.43-.991a6.932 6.932 0 010-.255c.007-.38-.138-.751-.43-.992l-1.004-.827a1.125 1.125 0 01-.26-1.43l1.297-2.247a1.125 1.125 0 011.37-.491l1.216.456c.356.133.751.072 1.076-.124.072-.044.146-.086.22-.128.332-.183.582-.495.644-.869l.214-1.28z" />
                                <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                            </svg>
                        </GzButton>
                    </div>
                </div>

                <GzSeparator />

                @* States *@
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">States</p>
                    <div class="flex flex-wrap items-center gap-3">
                        <GzButton Disabled="true">@L("Disabled", "معطل")</GzButton>
                        <GzButton Variant="ButtonVariant.Outline" Disabled="true">@L("Disabled Outline", "محيط معطل")</GzButton>
                        <GzButton Loading="true">@L("Loading...", "جاري التحميل...")</GzButton>
                        <GzButton Variant="ButtonVariant.Secondary" Loading="true">@L("Saving", "حفظ")</GzButton>
                        <GzButton Variant="ButtonVariant.Destructive" Loading="true">@L("Deleting", "حذف")</GzButton>
                    </div>
                </div>

                <GzSeparator />

                @* With Icon *@
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">With Icon</p>
                    <div class="flex flex-wrap items-center gap-3">
                        <GzButton>
                            <Icon>
                                <svg class="size-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                                </svg>
                            </Icon>
                            <ChildContent>New Project</ChildContent>
                        </GzButton>
                        <GzButton Variant="ButtonVariant.Outline">
                            <Icon>
                                <svg class="size-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12L12 16.5m0 0L7.5 12m4.5 4.5V3" />
                                </svg>
                            </Icon>
                            <ChildContent>Download</ChildContent>
                        </GzButton>
                    </div>
                </div>
            </div>
        </div>

        @* ─── Badge ──────────────────────────────────── *@
        <div id="badge" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Badge</h2>
                <p class="mt-1 text-sm text-muted-foreground">4 semantic variants for status indicators, labels, and counts. Solid variants feature inset-shadow depth.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="flex flex-wrap items-center gap-3">
                    <GzBadge>Default</GzBadge>
                    <GzBadge Variant="BadgeVariant.Secondary">Secondary</GzBadge>
                    <GzBadge Variant="BadgeVariant.Destructive">Destructive</GzBadge>
                    <GzBadge Variant="BadgeVariant.Outline">Outline</GzBadge>
                </div>
            </div>
        </div>

        @* ─── Separator ──────────────────────────────── *@
        <div id="separator" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Separator</h2>
                <p class="mt-1 text-sm text-muted-foreground">Subtle dividers with proper ARIA semantics. Decorative by default.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Horizontal</p>
                    <div class="rounded-lg bg-surface p-4">
                        <p class="text-sm text-foreground">Content above</p>
                        <GzSeparator class="my-3" />
                        <p class="text-sm text-foreground">Content below</p>
                    </div>
                </div>
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Vertical</p>
                    <div class="flex h-8 items-center gap-3 rounded-lg bg-surface px-4">
                        <span class="text-sm text-foreground">@L("Left", "يسار")</span>
                        <GzSeparator Orientation="SeparatorOrientation.Vertical" />
                        <span class="text-sm text-foreground">@L("Center", "وسط")</span>
                        <GzSeparator Orientation="SeparatorOrientation.Vertical" />
                        <span class="text-sm text-foreground">@L("Right", "يمين")</span>
                    </div>
                </div>
            </div>
        </div>

        @* ─── Input + Label ──────────────────────────── *@
        <div id="input" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Input + Label</h2>
                <p class="mt-1 text-sm text-muted-foreground">Text input with inset-ring border, validation states, icon slots, and two-way binding.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="grid max-w-sm gap-2">
                    <GzLabel For="demo-email" Required="true">@L("Email", "البريد الإلكتروني")</GzLabel>
                    <GzInput Id="demo-email" Type="email" Placeholder="@L("you@example.com", "البريد@مثال.كوم")" />
                </div>

                <div class="grid max-w-sm gap-2">
                    <GzLabel For="demo-password">@L("Password", "كلمة المرور")</GzLabel>
                    <GzInput Id="demo-password" Type="password" Placeholder="@L("Enter password", "أدخل كلمة المرور")" />
                </div>

                <div class="grid max-w-sm gap-2">
                    <GzLabel For="demo-error">@L("With validation error", "مع خطأ التحقق")</GzLabel>
                    <GzInput Id="demo-error" Type="email" Value="@L("not-an-email", "ليس-بريداً")" Error="@L("Please enter a valid email address", "يرجى إدخال عنوان بريد إلكتروني صالح")" />
                </div>

                <div class="grid max-w-sm gap-2">
                    <GzLabel For="demo-disabled">@L("Disabled", "معطل")</GzLabel>
                    <GzInput Id="demo-disabled" Placeholder="@L("Cannot type here", "لا يمكن الكتابة هنا")" Disabled="true" />
                </div>

                <div class="grid max-w-sm gap-2">
                    <GzLabel For="demo-file">@L("Upload file", "رفع ملف")</GzLabel>
                    <GzInput Id="demo-file" Type="file" />
                </div>
            </div>
        </div>

        @* ─── Textarea ───────────────────────────────── *@
        <div id="textarea" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Textarea</h2>
                <p class="mt-1 text-sm text-muted-foreground">Multi-line input with inset-ring border, validation, and configurable rows.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="grid max-w-lg gap-2">
                    <GzLabel For="demo-message" Required="true">Message</GzLabel>
                    <GzTextarea Id="demo-message" Placeholder="Tell us what you think..." Rows="4" />
                </div>

                <div class="grid max-w-lg gap-2">
                    <GzLabel For="demo-textarea-error">With validation error</GzLabel>
                    <GzTextarea Id="demo-textarea-error" Value="Too short" Error="Message must be at least 20 characters" />
                </div>

                <div class="grid max-w-lg gap-2">
                    <GzLabel For="demo-readonly">Read-only</GzLabel>
                    <GzTextarea Id="demo-readonly" Value="This content cannot be edited by the user." ReadOnly="true" />
                </div>
            </div>
        </div>

        @* ─── Checkbox ───────────────────────────────── *@
        <div id="checkbox" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Checkbox</h2>
                <p class="mt-1 text-sm text-muted-foreground">Custom checkbox with SVG checkmark, tactile press, and accessible focus ring.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzCheckbox @bind-Checked="_acceptTerms">@L("Accept terms and conditions", "قبول الشروط والأحكام")</GzCheckbox>
                <GzCheckbox @bind-Checked="_newsletter">@L("Subscribe to newsletter", "الاشتراك في النشرة البريدية")</GzCheckbox>
                <GzCheckbox Checked="true" Disabled="true">@L("Disabled (checked)", "معطل (محدد)")</GzCheckbox>
                <GzCheckbox Checked="false" Disabled="true">@L("Disabled (unchecked)", "معطل (غير محدد)")</GzCheckbox>
            </div>
        </div>

        @* ─── Radio ──────────────────────────────────── *@
        <div id="radio" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Radio</h2>
                <p class="mt-1 text-sm text-muted-foreground">Radio buttons with inner dot, group naming, and label support.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzRadio Name="plan" Value="free" Checked="@(_selectedPlan == "free")" CheckedChanged="@(_ => SelectPlan("free"))">@L("Free plan", "الخطة المجانية")</GzRadio>
                <GzRadio Name="plan" Value="pro" Checked="@(_selectedPlan == "pro")" CheckedChanged="@(_ => SelectPlan("pro"))">@L("Pro plan — $9/mo", "الخطة الاحترافية — $9/شهر")</GzRadio>
                <GzRadio Name="plan" Value="enterprise" Checked="@(_selectedPlan == "enterprise")" CheckedChanged="@(_ => SelectPlan("enterprise"))">@L("Enterprise — Contact us", "المؤسسات — تواصل معنا")</GzRadio>
                <GzRadio Name="disabled-group" Value="x" Checked="true" Disabled="true">@L("Disabled (selected)", "معطل (محدد)")</GzRadio>
            </div>
        </div>

        @* ─── Switch ─────────────────────────────────── *@
        <div id="switch" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Switch</h2>
                <p class="mt-1 text-sm text-muted-foreground">Toggle with sliding thumb, role="switch", and ARIA checked state.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzSwitch @bind-Checked="_notifications">@L("Enable notifications", "تفعيل الإشعارات")</GzSwitch>
                <GzSwitch @bind-Checked="_analytics">@L("Share analytics", "مشاركة التحليلات")</GzSwitch>
                <GzSwitch Checked="true" Disabled="true">@L("Always on (disabled)", "دائماً مفعل (معطل)")</GzSwitch>
                <GzSwitch Checked="false" Disabled="true">@L("Always off (disabled)", "دائماً موقف (معطل)")</GzSwitch>
            </div>
        </div>

        @* ─── Avatar ─────────────────────────────────── *@
        <div id="avatar" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Avatar</h2>
                <p class="mt-1 text-sm text-muted-foreground">Circular avatar with image, fallback initials, and default icon. Handles broken images gracefully.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">With image</p>
                    <div class="flex items-center gap-4">
                        <GzAvatar Src="https://api.dicebear.com/9.x/initials/svg?seed=JD" Alt="John Doe" Size="AvatarSize.Sm" />
                        <GzAvatar Src="https://api.dicebear.com/9.x/initials/svg?seed=AS" Alt="Alice Smith" Size="AvatarSize.Md" />
                        <GzAvatar Src="https://api.dicebear.com/9.x/initials/svg?seed=BW" Alt="Bob Wilson" Size="AvatarSize.Lg" />
                    </div>
                </div>
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Initials fallback</p>
                    <div class="flex items-center gap-4">
                        <GzAvatar Initials="JD" Alt="John Doe" Size="AvatarSize.Sm" />
                        <GzAvatar Initials="AS" Alt="Alice Smith" Size="AvatarSize.Md" />
                        <GzAvatar Initials="BW" Alt="Bob Wilson" Size="AvatarSize.Lg" />
                    </div>
                </div>
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Default icon</p>
                    <div class="flex items-center gap-4">
                        <GzAvatar Alt="User" Size="AvatarSize.Sm" />
                        <GzAvatar Alt="User" Size="AvatarSize.Md" />
                        <GzAvatar Alt="User" Size="AvatarSize.Lg" />
                    </div>
                </div>
            </div>
        </div>

        @* ─── Typography ─────────────────────────────── *@
        <div id="typography" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Typography</h2>
                <p class="mt-1 text-sm text-muted-foreground">Semantic HTML elements with predefined typographic scales. Renders correct heading levels, paragraphs, and inline code.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzTypography As="TypographyElement.H1">Heading 1</GzTypography>
                <GzTypography As="TypographyElement.H2">Heading 2</GzTypography>
                <GzTypography As="TypographyElement.H3">Heading 3</GzTypography>
                <GzTypography As="TypographyElement.H4">Heading 4</GzTypography>

                <GzSeparator class="my-4" />

                <GzTypography As="TypographyElement.P">This is a standard paragraph with default leading and foreground color. It's designed for body text and long-form content.</GzTypography>
                <GzTypography As="TypographyElement.Lead">This is a lead paragraph — larger, muted text for introductions.</GzTypography>
                <GzTypography As="TypographyElement.Large">Large text for emphasis</GzTypography>
                <GzTypography As="TypographyElement.Small">Small text for captions</GzTypography>
                <GzTypography As="TypographyElement.Muted">Muted text for secondary information</GzTypography>
                <GzTypography As="TypographyElement.P">Use <GzTypography As="TypographyElement.InlineCode">GzTypography</GzTypography> for consistent text styling.</GzTypography>
            </div>
        </div>

        @* ─── Skeleton ───────────────────────────────── *@
        <div id="skeleton" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Skeleton</h2>
                <p class="mt-1 text-sm text-muted-foreground">Animated loading placeholders. Line and circle variants. Override dimensions via class.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div>
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Card skeleton</p>
                    <div class="max-w-sm space-y-4 rounded-xl border border-border/50 p-6">
                        <div class="flex items-center gap-3">
                            <GzSkeleton Variant="SkeletonVariant.Circle" />
                            <div class="flex-1 space-y-2">
                                <GzSkeleton class="h-4 w-3/4" />
                                <GzSkeleton class="h-3 w-1/2" />
                            </div>
                        </div>
                        <GzSkeleton class="h-32 w-full" />
                        <div class="space-y-2">
                            <GzSkeleton />
                            <GzSkeleton class="w-4/5" />
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @* ─── Progress ───────────────────────────────── *@
        <div id="progress" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Progress</h2>
                <p class="mt-1 text-sm text-muted-foreground">Horizontal progress bar with smooth transition. 3 sizes, proper ARIA progressbar role.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="max-w-lg space-y-4">
                    <div>
                        <p class="mb-2 text-xs font-medium uppercase tracking-widest text-muted-foreground">Small (25%)</p>
                        <GzProgress Value="25" Size="ProgressSize.Sm" />
                    </div>
                    <div>
                        <p class="mb-2 text-xs font-medium uppercase tracking-widest text-muted-foreground">Medium (60%)</p>
                        <GzProgress Value="60" Size="ProgressSize.Md" />
                    </div>
                    <div>
                        <p class="mb-2 text-xs font-medium uppercase tracking-widest text-muted-foreground">Large (85%)</p>
                        <GzProgress Value="85" Size="ProgressSize.Lg" />
                    </div>
                </div>
            </div>
        </div>

        @* ─── Slider ──────────────────────────────────── *@
        <div id="slider" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Slider</h2>
                <p class="mt-1 text-sm text-muted-foreground">Range input with fill track, custom thumb, tick marks, and value display.</p>
            </div>

            <div class="space-y-8 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="max-w-md">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Basic</p>
                    <GzSlider @bind-Value="_sliderValue" Label="@L("Volume", "مستوى الصوت")" />
                </div>

                <GzSeparator />

                <div class="max-w-md">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">With Ticks</p>
                    <GzSlider @bind-Value="_sliderTicks" Min="0" Max="100" Step="25" ShowTicks="true" Label="@L("Quality", "الجودة")" />
                </div>

                <GzSeparator />

                <div class="max-w-md">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">Disabled</p>
                    <GzSlider Value="40" Disabled="true" Label="@L("Brightness", "السطوع")" />
                </div>
            </div>
        </div>

        @* ═══════════════════════════════════════════════ *@
        @* ═══       MOLECULES                        ═══ *@
        @* ═══════════════════════════════════════════════ *@

        <div class="pt-8">
            <GzSeparator />
            <p class="mt-6 text-xs font-medium uppercase tracking-widest text-primary">Molecules</p>
        </div>

        @* ─── Card ────────────────────────────────────── *@
        <div id="card" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Card</h2>
                <p class="mt-1 text-sm text-muted-foreground">Container with optional header, content, and footer slots. Clean border and shadow.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="grid gap-6 sm:grid-cols-2">
                    @* Basic card *@
                    <GzCard>
                        <Header>
                            <h3 class="text-base font-semibold text-foreground">Card Title</h3>
                            <p class="mt-1 text-sm text-muted-foreground">Card subtitle</p>
                        </Header>
                        <ChildContent>
                            <p class="text-sm text-muted-foreground">This is the card body content. Cards can hold any content — forms, lists, images, or text.</p>
                        </ChildContent>
                        <Footer>
                            <div class="flex justify-end gap-2">
                                <GzButton Variant="ButtonVariant.Ghost" Size="ButtonSize.Sm">Cancel</GzButton>
                                <GzButton Size="ButtonSize.Sm">Save</GzButton>
                            </div>
                        </Footer>
                    </GzCard>

                    @* Content-only card *@
                    <GzCard>
                        <ChildContent>
                            <div class="space-y-3">
                                <h3 class="text-base font-semibold text-foreground">Simple Card</h3>
                                <p class="text-sm text-muted-foreground">A card with content only — no header or footer. Useful for basic containers.</p>
                                <GzBadge Variant="BadgeVariant.Secondary">New</GzBadge>
                            </div>
                        </ChildContent>
                    </GzCard>
                </div>
            </div>
        </div>

        @* ─── Alert ───────────────────────────────────── *@
        <div id="alert" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Alert</h2>
                <p class="mt-1 text-sm text-muted-foreground">5 semantic variants with auto-icons, title, and description. Uses subtle tinted backgrounds.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzAlert Variant="AlertVariant.Default" Title="Default">
                    This is a default alert for general information.
                </GzAlert>
                <GzAlert Variant="AlertVariant.Info" Title="Info">
                    Your workspace is syncing. This may take a few seconds.
                </GzAlert>
                <GzAlert Variant="AlertVariant.Success" Title="Success">
                    Your changes have been saved successfully.
                </GzAlert>
                <GzAlert Variant="AlertVariant.Warning" Title="Warning">
                    Your trial expires in 3 days. Upgrade to keep your data.
                </GzAlert>
                <GzAlert Variant="AlertVariant.Destructive" Title="Error">
                    Failed to deploy. Check your build logs for details.
                </GzAlert>
            </div>
        </div>

        @* ─── FormField ──────────────────────────────── *@
        <div id="formfield" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">FormField</h2>
                <p class="mt-1 text-sm text-muted-foreground">Composes label + control + description/error into a single wrapper. Error replaces description when set.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="max-w-sm">
                    <GzFormField Label="Username" Id="ff-username" Description="This is your public display name." Required="true">
                        <GzInput Id="ff-username" Placeholder="glazeui" />
                    </GzFormField>
                </div>

                <div class="max-w-sm">
                    <GzFormField Label="Email" Id="ff-email" Error="Please enter a valid email address." Required="true">
                        <GzInput Id="ff-email" Type="email" Value="invalid-email" />
                    </GzFormField>
                </div>

                <div class="max-w-lg">
                    <GzFormField Label="Bio" Id="ff-bio" Description="Write a short introduction about yourself.">
                        <GzTextarea Id="ff-bio" Placeholder="Tell us about yourself..." Rows="3" />
                    </GzFormField>
                </div>
            </div>
        </div>

        @* ─── Tabs ────────────────────────────────────── *@
        <div id="tabs" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Tabs</h2>
                <p class="mt-1 text-sm text-muted-foreground">Tab navigation with ARIA tablist/tab/tabpanel pattern. Active tab has elevated background.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzTabs Tabs="_demoPanels" @bind-ActiveTab="_activeTab" />
            </div>
        </div>

        @* ─── Tooltip ─────────────────────────────────── *@
        <div id="tooltip" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Tooltip</h2>
                <p class="mt-1 text-sm text-muted-foreground">CSS-only tooltip with 4 positions. Zero JS, zero SignalR roundtrips — instant on hover and focus.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="flex flex-wrap items-center gap-6">
                    <GzTooltip Text="Appears on top" Position="TooltipPosition.Top">
                        <GzButton Variant="ButtonVariant.Outline">Top</GzButton>
                    </GzTooltip>
                    <GzTooltip Text="Appears on the right" Position="TooltipPosition.Right">
                        <GzButton Variant="ButtonVariant.Outline">Right</GzButton>
                    </GzTooltip>
                    <GzTooltip Text="Appears on the bottom" Position="TooltipPosition.Bottom">
                        <GzButton Variant="ButtonVariant.Outline">Bottom</GzButton>
                    </GzTooltip>
                    <GzTooltip Text="Appears on the left" Position="TooltipPosition.Left">
                        <GzButton Variant="ButtonVariant.Outline">Left</GzButton>
                    </GzTooltip>
                </div>
            </div>
        </div>

        @* ─── Select ──────────────────────────────────── *@
        <div id="select" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Select</h2>
                <p class="mt-1 text-sm text-muted-foreground">Dropdown select with keyboard navigation (arrow keys, enter, escape), ARIA combobox, and checkmark indicator.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="max-w-sm">
                    <GzFormField Label="Framework" Id="ff-framework" Description="Your preferred frontend framework.">
                        <GzSelect Options="_frameworkOptions" @bind-Value="_selectedFramework" Placeholder="Select a framework" />
                    </GzFormField>
                </div>
                <div class="max-w-sm">
                    <GzFormField Label="Disabled" Id="ff-disabled-select">
                        <GzSelect Options="_frameworkOptions" Placeholder="Cannot change" Disabled="true" />
                    </GzFormField>
                </div>
            </div>
        </div>

        @* ─── Accordion ───────────────────────────────── *@
        <div id="accordion" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Accordion</h2>
                <p class="mt-1 text-sm text-muted-foreground">Collapsible content sections with chevron rotation and hover state.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzAccordion>
                    <GzAccordionItem Title="@L("Is GlazeUI free?", "هل GlazeUI مجاني؟")" DefaultOpen="true">
                        @L("Yes. GlazeUI is open-source under the Apache 2.0 license. Copy components into your project and modify everything.",
                           "نعم. GlazeUI مفتوح المصدر تحت رخصة Apache 2.0. انسخ المكونات إلى مشروعك وعدّل كل شيء.")
                    </GzAccordionItem>
                    <GzAccordionItem Title="@L("Can I use it with Blazor WASM?", "هل يمكنني استخدامه مع Blazor WASM؟")">
                        @L("GlazeUI is built for Blazor Server but the Razor components work in any Blazor hosting model — Server, WASM, or Hybrid.",
                           "تم بناء GlazeUI لـ Blazor Server لكن مكونات Razor تعمل في أي نموذج استضافة — Server أو WASM أو Hybrid.")
                    </GzAccordionItem>
                    <GzAccordionItem Title="@L("Does it support dark mode?", "هل يدعم الوضع الداكن؟")">
                        @L("Yes. All tokens have light and dark values.",
                           "نعم. جميع الرموز لها قيم فاتحة وداكنة.")
                    </GzAccordionItem>
                </GzAccordion>
            </div>
        </div>

        @* ─── Breadcrumb ──────────────────────────────── *@
        <div id="breadcrumb" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Breadcrumb</h2>
                <p class="mt-1 text-sm text-muted-foreground">Navigation trail with separator chevrons and RTL-safe rotation.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzBreadcrumb Items="@ActiveBreadcrumbItems" />
            </div>
        </div>

        @* ─── FileUpload ───────────────────────────────── *@
        <div id="fileupload" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">FileUpload</h2>
                <p class="mt-1 text-sm text-muted-foreground">Drag-and-drop file upload zone with file list, type/size validation, and remove controls.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="max-w-lg">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">@L("Single File", "ملف واحد")</p>
                    <GzFileUpload Accept=".pdf,.docx,.txt" MaxSizeBytes="10485760" />
                </div>

                <GzSeparator />

                <div class="max-w-lg">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">@L("Multiple Files", "ملفات متعددة")</p>
                    <GzFileUpload Accept="image/*" Multiple="true" MaxSizeBytes="5242880" />
                </div>
            </div>
        </div>

        @* ─── Stepper ─────────────────────────────────── *@
        <div id="stepper" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Stepper</h2>
                <p class="mt-1 text-sm text-muted-foreground">Multi-step wizard with numbered progress indicators, connectors, and navigation buttons.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzStepper Steps="@ActiveStepperSteps" @bind-ActiveStep="_activeStepperStep" />
            </div>
        </div>

        @* ═══════════════════════════════════════════════ *@
        @* ═══       ORGANISMS                        ═══ *@
        @* ═══════════════════════════════════════════════ *@

        <div class="pt-8">
            <GzSeparator />
            <p class="mt-6 text-xs font-medium uppercase tracking-widest text-primary">Organisms</p>
        </div>

        @* ─── Dialog ──────────────────────────────────── *@
        <div id="dialog" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Dialog</h2>
                <p class="mt-1 text-sm text-muted-foreground">Modal with backdrop blur, click-outside-to-close, X button, Header/Footer slots, 4 sizes, and accessible ARIA.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="flex flex-wrap gap-3">
                    <GzButton @onclick="() => _dialogOpen = true">Open Dialog</GzButton>
                    <GzButton Variant="ButtonVariant.Outline" @onclick="() => _dialogSmOpen = true">Small Dialog</GzButton>
                </div>
            </div>

            <GzDialog @bind-Open="_dialogOpen" Title="Edit Profile" Description="Make changes to your profile here. Click save when you're done.">
                <ChildContent>
                    <div class="space-y-4">
                        <GzFormField Label="Name" Id="dlg-name">
                            <GzInput Id="dlg-name" Placeholder="John Doe" />
                        </GzFormField>
                        <GzFormField Label="Email" Id="dlg-email">
                            <GzInput Id="dlg-email" Type="email" Placeholder="john@example.com" />
                        </GzFormField>
                    </div>
                </ChildContent>
                <Footer>
                    <GzButton Variant="ButtonVariant.Ghost" @onclick="() => _dialogOpen = false">Cancel</GzButton>
                    <GzButton @onclick="() => _dialogOpen = false">Save Changes</GzButton>
                </Footer>
            </GzDialog>

            <GzDialog @bind-Open="_dialogSmOpen" Title="Confirm Action" Description="Are you sure you want to continue?" Size="DialogSize.Sm">
                <Footer>
                    <GzButton Variant="ButtonVariant.Ghost" @onclick="() => _dialogSmOpen = false">Cancel</GzButton>
                    <GzButton Variant="ButtonVariant.Destructive" @onclick="() => _dialogSmOpen = false">Delete</GzButton>
                </Footer>
            </GzDialog>
        </div>

        @* ─── DropdownMenu ────────────────────────────── *@
        <div id="dropdown" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">DropdownMenu</h2>
                <p class="mt-1 text-sm text-muted-foreground">Trigger + floating menu with items, separators, keyboard shortcut labels, and disabled state.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzDropdownMenu>
                    <Trigger>
                        <GzButton Variant="ButtonVariant.Outline">
                            <ChildContent>Open Menu</ChildContent>
                        </GzButton>
                    </Trigger>
                    <ChildContent>
                        <GzDropdownMenuItem Shortcut="⌘N">New File</GzDropdownMenuItem>
                        <GzDropdownMenuItem Shortcut="⌘O">Open</GzDropdownMenuItem>
                        <GzSeparator class="my-1" />
                        <GzDropdownMenuItem Shortcut="⌘S">Save</GzDropdownMenuItem>
                        <GzDropdownMenuItem Shortcut="⇧⌘S">Save As</GzDropdownMenuItem>
                        <GzSeparator class="my-1" />
                        <GzDropdownMenuItem Disabled="true">Print</GzDropdownMenuItem>
                    </ChildContent>
                </GzDropdownMenu>
            </div>
        </div>

        @* ─── Popover ─────────────────────────────────── *@
        <div id="popover" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Popover</h2>
                <p class="mt-1 text-sm text-muted-foreground">Click-triggered floating panel. General-purpose — any content can go inside.</p>
            </div>

            <div class="rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzPopover Align="start">
                    <Trigger>
                        <GzButton Variant="ButtonVariant.Outline">Open Popover</GzButton>
                    </Trigger>
                    <ChildContent>
                        <div class="space-y-4">
                            <div>
                                <h4 class="text-sm font-semibold text-foreground">Dimensions</h4>
                                <p class="text-xs text-muted-foreground">Set the dimensions for the layer.</p>
                            </div>
                            <div class="grid grid-cols-2 gap-3">
                                <GzFormField Label="Width" Id="pop-w">
                                    <GzInput Id="pop-w" Placeholder="100%" />
                                </GzFormField>
                                <GzFormField Label="Height" Id="pop-h">
                                    <GzInput Id="pop-h" Placeholder="auto" />
                                </GzFormField>
                            </div>
                        </div>
                    </ChildContent>
                </GzPopover>
            </div>
        </div>
        @* ─── Toast ───────────────────────────────────── *@
        <div id="toast" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Toast</h2>
                <p class="mt-1 text-sm text-muted-foreground">Temporary notifications with auto-dismiss, close button, and 3 variants.</p>
            </div>

            <div class="space-y-4 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="flex flex-wrap gap-3">
                    <GzButton Size="ButtonSize.Sm" OnClick="@(() => _toastDefault = true)">Show Default</GzButton>
                    <GzButton Size="ButtonSize.Sm" OnClick="@(() => _toastSuccess = true)">Show Success</GzButton>
                    <GzButton Size="ButtonSize.Sm" Variant="ButtonVariant.Destructive" OnClick="@(() => _toastError = true)">Show Error</GzButton>
                </div>
                <div class="space-y-3">
                    <GzToast Title="Event created" @bind-Visible="_toastDefault" Duration="0">
                        Your event has been scheduled for tomorrow at 3pm.
                    </GzToast>
                    <GzToast Title="Changes saved" Variant="ToastVariant.Success" @bind-Visible="_toastSuccess" Duration="0">
                        Your profile has been updated successfully.
                    </GzToast>
                    <GzToast Title="Delete failed" Variant="ToastVariant.Error" @bind-Visible="_toastError" Duration="0">
                        Something went wrong. Please try again.
                    </GzToast>
                </div>
            </div>
        </div>

        @* ─── Combobox ────────────────────────────────── *@
        <div id="combobox" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">Combobox</h2>
                <p class="mt-1 text-sm text-muted-foreground">Searchable select with type-to-filter, keyboard navigation, and checkmark indicator.</p>
            </div>

            <div class="max-w-sm rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <GzCombobox Options="@ActiveComboboxOptions"
                            @bind-Value="_selectedCountry"
                            Placeholder="@L("Search countries...", "ابحث عن الدول...")" />
                @if (!string.IsNullOrEmpty(_selectedCountry))
                {
                    <p class="mt-3 text-sm text-muted-foreground">Selected: <strong class="text-foreground">@_selectedCountry</strong></p>
                }
            </div>
        </div>

        @* ─── DataTable ───────────────────────────────── *@
        <div id="datatable" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">DataTable</h2>
                <p class="mt-1 text-sm text-muted-foreground">Sortable, filterable, paginated data grid with keyboard-sortable columns.</p>
            </div>

            <GzDataTable TItem="UserRecord"
                         Items="@_tableData"
                         Columns="@ActiveTableColumns"
                         PageSize="5"
                         Filterable="true"
                         FilterPlaceholder="@L("Search users...", "ابحث عن المستخدمين...")" />
        </div>

        @* ─── DatePicker ───────────────────────────────── *@
        <div id="datepicker" class="scroll-mt-24">
            <div class="mb-6">
                <h2 class="text-xl font-semibold tracking-tight text-foreground">DatePicker</h2>
                <p class="mt-1 text-sm text-muted-foreground">Calendar dropdown with month/year navigation, min/max date constraints, keyboard navigation, and clearable selection.</p>
            </div>

            <div class="space-y-6 rounded-xl border border-border/50 bg-card p-6 shadow-xs sm:p-8">
                <div class="max-w-sm">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">@L("Basic", "أساسي")</p>
                    <GzDatePicker @bind-Value="_selectedDate" Placeholder="@L("Pick a date", "اختر تاريخاً")" />
                    @if (_selectedDate.HasValue)
                    {
                        <p class="mt-2 text-sm text-muted-foreground">Selected: <strong class="text-foreground">@_selectedDate.Value.ToString("yyyy-MM-dd")</strong></p>
                    }
                </div>

                <GzSeparator />

                <div class="max-w-sm">
                    <p class="mb-3 text-xs font-medium uppercase tracking-widest text-muted-foreground">@L("Disabled", "معطل")</p>
                    <GzDatePicker Disabled="true" Placeholder="@L("Cannot select", "لا يمكن الاختيار")" />
                </div>
            </div>
        </div>

        </div> @* end flex-1 *@
    </div>
</section>

@code {
    private bool _acceptTerms;
    private bool _newsletter = true;
    private bool _notifications = true;
    private bool _analytics;
    private string _selectedPlan = "pro";
    private string? _activeTab;
    private bool _isRtl;
    private bool _dialogOpen;
    private bool _dialogSmOpen;
    private string? _selectedFramework;

    private List<TabItem> _demoPanels = new();

    private readonly List<SelectOption> _frameworkOptions = new()
    {
        new() { Value = "blazor", Label = "Blazor" },
        new() { Value = "react", Label = "React" },
        new() { Value = "vue", Label = "Vue" },
        new() { Value = "angular", Label = "Angular" },
        new() { Value = "svelte", Label = "Svelte", Disabled = true }
    };

    protected override void OnInitialized()
    {
        _demoPanels = new List<TabItem>
        {
            new TabItem
            {
                Key = "account",
                Label = "Account",
                Content = @<div class="space-y-4">
                    <GzFormField Label="Name" Id="tab-name" Description="Your full name as shown on your profile.">
                        <GzInput Id="tab-name" Placeholder="John Doe" />
                    </GzFormField>
                    <GzFormField Label="Email" Id="tab-email">
                        <GzInput Id="tab-email" Type="email" Placeholder="john@example.com" />
                    </GzFormField>
                    <GzButton Size="ButtonSize.Sm">Save Changes</GzButton>
                </div>
            },
            new TabItem
            {
                Key = "password",
                Label = "Password",
                Content = @<div class="space-y-4">
                    <GzFormField Label="Current Password" Id="tab-current-pw">
                        <GzInput Id="tab-current-pw" Type="password" Placeholder="Enter current password" />
                    </GzFormField>
                    <GzFormField Label="New Password" Id="tab-new-pw">
                        <GzInput Id="tab-new-pw" Type="password" Placeholder="Enter new password" />
                    </GzFormField>
                    <GzButton Size="ButtonSize.Sm">Update Password</GzButton>
                </div>
            },
            new TabItem
            {
                Key = "notifications",
                Label = "Notifications",
                Content = @<div class="space-y-4">
                    <GzSwitch @bind-Checked="_notifications">Email notifications</GzSwitch>
                    <GzSwitch @bind-Checked="_analytics">Marketing emails</GzSwitch>
                    <GzSwitch Checked="true" Disabled="true">Security alerts (always on)</GzSwitch>
                </div>
            }
        };
    }

    private void SelectPlan(string plan)
    {
        _selectedPlan = plan;
    }

    // ─── Breadcrumb ──────────────────────────────────
    private readonly List<BreadcrumbItem> _breadcrumbItems = new()
    {
        new() { Label = "Home", Href = "/" },
        new() { Label = "Docs", Href = "/docs" },
        new() { Label = "Components", Href = "/components" },
        new() { Label = "Breadcrumb" }
    };

    // ─── Toast ───────────────────────────────────────
    private bool _toastDefault;
    private bool _toastSuccess;
    private bool _toastError;

    // ─── Slider ──────────────────────────────────────
    private double _sliderValue = 50;
    private double _sliderTicks = 50;

    // ─── Stepper ─────────────────────────────────────
    private int _activeStepperStep;
    private readonly List<StepItem> _stepperSteps = new()
    {
        new() { Label = "Account", Description = "Create your account" },
        new() { Label = "Profile", Description = "Set up your profile" },
        new() { Label = "Review", Description = "Confirm your details" },
    };

    // ─── DatePicker ───────────────────────────────────
    private DateOnly? _selectedDate;

    // ─── Combobox ────────────────────────────────────
    private string? _selectedCountry;
    private readonly List<SelectOption> _comboboxOptions = new()
    {
        new() { Value = "us", Label = "United States" },
        new() { Value = "gb", Label = "United Kingdom" },
        new() { Value = "de", Label = "Germany" },
        new() { Value = "fr", Label = "France" },
        new() { Value = "jp", Label = "Japan" },
        new() { Value = "au", Label = "Australia" },
        new() { Value = "ca", Label = "Canada" },
        new() { Value = "br", Label = "Brazil" },
        new() { Value = "in", Label = "India" },
        new() { Value = "kr", Label = "South Korea" },
    };

    // ─── DataTable ───────────────────────────────────
    private record UserRecord(string Name, string Email, string Role, string Status);

    private readonly List<DataTableColumn<UserRecord>> _tableColumns = new()
    {
        new() { Header = "Name", Field = x => x.Name },
        new() { Header = "Email", Field = x => x.Email },
        new() { Header = "Role", Field = x => x.Role },
        new() { Header = "Status", Field = x => x.Status },
    };

    private readonly List<UserRecord> _tableData = new()
    {
        new("Alice Smith", "alice@acme.com", "Admin", "Active"),
        new("Bob Wilson", "bob@acme.com", "Editor", "Active"),
        new("Carol Lee", "carol@acme.com", "Viewer", "Inactive"),
        new("Dave Brown", "dave@acme.com", "Editor", "Active"),
        new("Eve Johnson", "eve@acme.com", "Admin", "Active"),
        new("Frank Garcia", "frank@acme.com", "Viewer", "Invited"),
        new("Grace Kim", "grace@acme.com", "Editor", "Active"),
        new("Henry Liu", "henry@acme.com", "Viewer", "Inactive"),
        new("Iris Chen", "iris@acme.com", "Admin", "Active"),
        new("Jack Davis", "jack@acme.com", "Editor", "Active"),
        new("Karen Wang", "karen@acme.com", "Viewer", "Active"),
        new("Leo Martinez", "leo@acme.com", "Editor", "Invited"),
    };

    // ─── RTL Arabic Localization Helper ──────────────
    /// <summary>Returns Arabic text when in RTL mode, otherwise the English default.</summary>
    private string L(string en, string ar) => _isRtl ? ar : en;

    private List<BreadcrumbItem> ActiveBreadcrumbItems => _isRtl
        ? new()
        {
            new() { Label = "الرئيسية", Href = "/" },
            new() { Label = "التوثيق", Href = "/docs" },
            new() { Label = "المكونات", Href = "/components" },
            new() { Label = "مسار التنقل" }
        }
        : _breadcrumbItems;

    private List<SelectOption> ActiveComboboxOptions => _isRtl
        ? new()
        {
            new() { Value = "sa", Label = "المملكة العربية السعودية" },
            new() { Value = "ae", Label = "الإمارات العربية المتحدة" },
            new() { Value = "eg", Label = "مصر" },
            new() { Value = "ma", Label = "المغرب" },
            new() { Value = "jo", Label = "الأردن" },
            new() { Value = "kw", Label = "الكويت" },
            new() { Value = "qa", Label = "قطر" },
            new() { Value = "lb", Label = "لبنان" },
            new() { Value = "iq", Label = "العراق" },
            new() { Value = "tn", Label = "تونس" },
        }
        : _comboboxOptions;

    private List<DataTableColumn<UserRecord>> ActiveTableColumns => _isRtl
        ? new()
        {
            new() { Header = "الاسم", Field = x => x.Name },
            new() { Header = "البريد الإلكتروني", Field = x => x.Email },
            new() { Header = "الدور", Field = x => x.Role },
            new() { Header = "الحالة", Field = x => x.Status },
        }
        : _tableColumns;

    private List<StepItem> ActiveStepperSteps => _isRtl
        ? new()
        {
            new() { Label = "الحساب", Description = "أنشئ حسابك" },
            new() { Label = "الملف الشخصي", Description = "أعد ملفك الشخصي" },
            new() { Label = "المراجعة", Description = "أكد تفاصيلك" },
        }
        : _stepperSteps;
}
